import { completedCount } from 'ddlisting/helpers/completed-count';
import { module, test } from 'qunit';

module('Unit | Helper | completed count');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = completedCount([42]);
  assert.ok(result);
});
