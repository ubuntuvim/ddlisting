import { filterCategoryItemByStatus } from 'ddlisting/helpers/filter-category-item-by-status';
import { module, test } from 'qunit';

module('Unit | Helper | filter category item by status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filterCategoryItemByStatus([42]);
  assert.ok(result);
});
