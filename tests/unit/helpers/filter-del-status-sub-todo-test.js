import { filterDelStatusSubTodo } from 'ddlisting/helpers/filter-del-status-sub-todo';
import { module, test } from 'qunit';

module('Unit | Helper | filter del status sub todo');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filterDelStatusSubTodo([42]);
  assert.ok(result);
});
