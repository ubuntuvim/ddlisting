import { filterProjectByStatus } from 'ddlisting/helpers/filter-project-by-status';
import { module, test } from 'qunit';

module('Unit | Helper | filter project by status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filterProjectByStatus([42]);
  assert.ok(result);
});
