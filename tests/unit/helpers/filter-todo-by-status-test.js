import { filterTodoByStatus } from 'ddlisting/helpers/filter-todo-by-status';
import { module, test } from 'qunit';

module('Unit | Helper | filter todo by status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filterTodoByStatus([42]);
  assert.ok(result);
});
