import { indexPlusOne } from 'ddlisting/helpers/index-plus-one';
import { module, test } from 'qunit';

module('Unit | Helper | index plus one');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = indexPlusOne([42]);
  assert.ok(result);
});
