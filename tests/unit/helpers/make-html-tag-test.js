import { makeHtmlTag } from 'ddlisting/helpers/make-html-tag';
import { module, test } from 'qunit';

module('Unit | Helper | make html tag');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = makeHtmlTag([42]);
  assert.ok(result);
});
