import { multilayerif } from 'ddlisting/helpers/multilayerif';
import { module, test } from 'qunit';

module('Unit | Helper | multilayerif');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = multilayerif([42]);
  assert.ok(result);
});
