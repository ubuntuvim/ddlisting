import { notCompletedCount } from 'ddlisting/helpers/not-completed-count';
import { module, test } from 'qunit';

module('Unit | Helper | not completed count');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = notCompletedCount([42]);
  assert.ok(result);
});
