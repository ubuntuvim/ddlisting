import { obtainNameByUserid } from 'ddlisting/helpers/obtain-name-by-userid';
import { module, test } from 'qunit';

module('Unit | Helper | obtain name by userid');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = obtainNameByUserid([42]);
  assert.ok(result);
});
