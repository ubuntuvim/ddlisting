import { sortModelList } from 'ddlisting/helpers/sort-model-list';
import { module, test } from 'qunit';

module('Unit | Helper | sort model list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sortModelList([42]);
  assert.ok(result);
});
