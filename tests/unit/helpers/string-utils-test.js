import { stringUtils } from 'ddlisting/helpers/string-utils';
import { module, test } from 'qunit';

module('Unit | Helper | string utils');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stringUtils([42]);
  assert.ok(result);
});
