import completedTodo from 'ddlisting/utils/completed-todo';
import { module, test } from 'qunit';

module('Unit | Utility | completed todo');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = completedTodo();
  assert.ok(result);
});
