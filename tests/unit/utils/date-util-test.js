import dateUtil from 'ddlisting/utils/date-util';
import { module, test } from 'qunit';

module('Unit | Utility | date util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateUtil();
  assert.ok(result);
});
