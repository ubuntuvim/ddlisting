import getUserFromSession from 'ddlisting/utils/get-user-from-session';
import { module, test } from 'qunit';

module('Unit | Utility | get user from session');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getUserFromSession();
  assert.ok(result);
});
