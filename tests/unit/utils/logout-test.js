import logout from 'ddlisting/utils/logout';
import { module, test } from 'qunit';

module('Unit | Utility | logout');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = logout();
  assert.ok(result);
});
