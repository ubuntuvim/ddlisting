import playCompletedBGM from 'ddlisting/utils/play-completed-bgm';
import { module, test } from 'qunit';

module('Unit | Utility | play completed bgm');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = playCompletedBGM();
  assert.ok(result);
});
