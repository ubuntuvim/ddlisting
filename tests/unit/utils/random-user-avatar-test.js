import randomUserAvatar from 'ddlisting/utils/random-user-avatar';
import { module, test } from 'qunit';

module('Unit | Utility | random user avatar');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = randomUserAvatar();
  assert.ok(result);
});
