import setGreatedLiked from 'ddlisting/utils/set-greated-liked';
import { module, test } from 'qunit';

module('Unit | Utility | set greated liked');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = setGreatedLiked();
  assert.ok(result);
});
