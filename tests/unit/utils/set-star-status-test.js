import setStarStatus from 'ddlisting/utils/set-star-status';
import { module, test } from 'qunit';

module('Unit | Utility | set star status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = setStarStatus();
  assert.ok(result);
});
